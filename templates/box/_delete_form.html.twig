<form method="post" action="{{ path('app_box_delete', {'id': box.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ box.id) }}">
    <button class="btn">Delete</button>
</form>
