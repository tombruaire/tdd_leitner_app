{% extends 'base.html.twig' %}

{% block title %}Box{% endblock %}

{% block body %}
    <h1>Box</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ box.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_box_index') }}">back to list</a>

    <a href="{{ path('app_box_edit', {'id': box.id}) }}">edit</a>

    {{ include('box/_delete_form.html.twig') }}
{% endblock %}
